apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frans.fullname" . }}-test-connection"
  labels:
    {{- include "frans.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frans.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
